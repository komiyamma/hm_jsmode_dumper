execjs macrodir + @"\jsmode_modules\dumper.js";
js {

    var obj = {
        a:3,
        b:"abc",
        e : {
            f : 3,
            g : function(a) { /* あ
    　いう     */
            }
        }
    };

    debuginfo(2);

    Dumper.dir(obj);

    Dumper.dir({});

    var l = Dumper.stringify(lineno);
    console.log(l);

    var f = Dumper.stringify(lineno());
    console.log(f);
}


execjs macrodir + @"\jsmode_modules\require.js";

js {
    var Dumper = require("dumper");
    var o = Dumper.stringify(null);
    console.log(o);

    Dumper.dir(undefined);
    Dumper.dir("345");
    Dumper.dir(true);

    var obj3 = {
        f : obj3
    }

    Dumper.dir(obj3);
}