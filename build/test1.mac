execjs macrodir + @"\jsmode_modules\stringify.js";
js {

    var obj = {
        a:3,
        b:"abc",
        e : {
            f : 3,
            g : function(a) { /* あ
    　いう     */
            }
        }
    };

    debuginfo(2);

    stringify.dir(obj);

    stringify.dir({});

    var l = stringify(lineno);
    console.log(l);

    var f = stringify(lineno());
    console.log(f);
}


execjs macrodir + @"\jsmode_modules\require.js";

js {
    var Dump = require("stringify");
    var o = Dump.stringify(null);
    console.log(o);

    Dump.dir(undefined);
    Dump.dir("345");
    Dump.dir(true);

    var obj3 = {
        f : obj3
    }

    Dump.dir(obj3);
}